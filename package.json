{
  "name": "healway",
  "private": true,
  "engines": {
    "yarn": ">=1.7.0 <2",
    "node": ">=18"
  },
  "scripts": {
    "link-env:local": "bun link-envs.ts local",
    "link-env:prod": "bun link-envs.ts prod",
    "build": "turbo run build",
    "dev": "turbo run dev",
    "start": "turbo run start"
  },
  "devDependencies": {
    "turbo": "^2.4.2"
  },
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "apps/*"
  ]
}
